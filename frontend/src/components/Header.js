import React from 'react';\nimport {\n  AppBar, Toolbar, Typography, IconButton, Badge, Avatar,\n  Box, Chip, Tooltip\n} from '@mui/material';\nimport {\n  Menu as MenuIcon, Notifications, AccountCircle,\n  Psychology, TrendingUp\n} from '@mui/icons-material';\nimport { motion } from 'framer-motion';\n\nconst Header = ({ drawerOpen, setDrawerOpen, currentView }) => {\n  const getViewTitle = () => {\n    switch (currentView) {\n      case 'dashboard': return 'Healthcare Dashboard';\n      case 'search': return 'Patient Search & Analysis';\n      case 'analytics': return 'Medical Analytics';\n      case 'ai-insights': return 'AI Medical Insights';\n      default: return 'EHR System';\n    }\n  };\n\n  const getViewIcon = () => {\n    switch (currentView) {\n      case 'ai-insights': return <Psychology sx={{ mr: 1, color: '#f59e0b' }} />;\n      case 'analytics': return <TrendingUp sx={{ mr: 1, color: '#10b981' }} />;\n      default: return null;\n    }\n  };\n\n  return (\n    <AppBar \n      position=\"sticky\" \n      elevation={0}\n      sx={{ \n        bgcolor: 'background.paper',\n        borderBottom: '1px solid',\n        borderColor: 'divider',\n        color: 'text.primary'\n      }}\n    >\n      <Toolbar sx={{ px: 3 }}>\n        <IconButton\n          edge=\"start\"\n          onClick={() => setDrawerOpen(!drawerOpen)}\n          sx={{ mr: 2, color: 'text.primary' }}\n        >\n          <MenuIcon />\n        </IconButton>\n\n        <Box sx={{ display: 'flex', alignItems: 'center', flexGrow: 1 }}>\n          {getViewIcon()}\n          <motion.div\n            key={currentView}\n            initial={{ opacity: 0, x: -20 }}\n            animate={{ opacity: 1, x: 0 }}\n            transition={{ duration: 0.3 }}\n          >\n            <Typography variant=\"h6\" sx={{ fontWeight: 600 }}>\n              {getViewTitle()}\n            </Typography>\n          </motion.div>\n        </Box>\n\n        <Box sx={{ display: 'flex', alignItems: 'center', gap: 2 }}>\n          <Chip \n            label=\"AI Active\" \n            size=\"small\" \n            sx={{ \n              bgcolor: '#dcfce7', \n              color: '#166534',\n              fontWeight: 600,\n              '& .MuiChip-label': {\n                px: 1.5\n              }\n            }}\n          />\n          \n          <Tooltip title=\"Notifications\">\n            <IconButton>\n              <Badge badgeContent={3} color=\"error\">\n                <Notifications />\n              </Badge>\n            </IconButton>\n          </Tooltip>\n\n          <Tooltip title=\"Profile\">\n            <Avatar \n              sx={{ \n                width: 36, \n                height: 36,\n                bgcolor: 'primary.main',\n                cursor: 'pointer'\n              }}\n            >\n              <AccountCircle />\n            </Avatar>\n          </Tooltip>\n        </Box>\n      </Toolbar>\n    </AppBar>\n  );\n};\n\nexport default Header;